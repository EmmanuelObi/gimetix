import React from 'react';

const profile = () => {
  return (
    <div
      style={{
        background: '#fff',
        color: 'black',
        minHeight: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Profile In Progress
    </div>
  );
};

export default profile;
