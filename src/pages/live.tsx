import React from 'react';

const live = () => {
  return (
    <div
      style={{
        background: '#fff',
        color: 'black',
        minHeight: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      LiveStream In Progress
    </div>
  );
};

export default live;
