import React from 'react';

const wallet = () => {
  return (
    <div
      style={{
        background: '#fff',
        color: 'black',
        minHeight: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Wallet In Progress
    </div>
  );
};

export default wallet;
