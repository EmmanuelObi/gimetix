import React from 'react';

const tickets = () => {
  return (
    <div
      style={{
        background: '#fff',
        color: 'black',
        minHeight: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Tickets In Progress
    </div>
  );
};

export default tickets;
